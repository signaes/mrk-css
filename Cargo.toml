[package]
name = "mrk-css"
version = "0.1.4"
edition = "2024"
description = "Type-safe CSS authoring: StyleSheet, rules, at-rules, selectors, typed values, the CSS Color 4 parser, a pretty-printer, and the css! macro"
authors = ["Thiago Oliveira <thiago@signaes.com>"]
license = "MIT"
repository = "https://github.com/signaes/mrk-css"
homepage = "https://github.com/signaes/mrk-css"
keywords = ["css", "markup", "builder", "stylesheet"]
categories = ["template-engine"]

[dependencies]
mrk = { version = "0.9.0" }
